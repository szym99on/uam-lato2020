package pl.psi.game.hero;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

public class CreatureRequest {
//PW use factory in correct way. Test should have some assertion. I cannot recognize what you want to check here... Creature is never added to list? Correct or remove.
    @Test
    @Disabled
    void xxx(){
//        CreatureInfo creature = CreatureInfoFactory.getCreature(CreatureInfoFactory.SKELETON);
//            Map<CreatureInfo, Integer> creatures = new HashMap<>();
//            creatures.put(creature.getName(),creature.getCost());
    }
}